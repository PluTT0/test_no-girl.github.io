document.querySelectorAll('.video__wrapper').forEach((div) =>  {const video = div.querySelector('video');const bplay = div.querySelector('.play');video.addEventListener('click', ev => {if (video.paused) return;video.pause();bplay.classList.remove('hidden');});video.addEventListener('ended', ev => {video.currentTime = 0;bplay.classList.remove('hidden');});bplay.addEventListener('click', ev => {if (! video.paused) return;video.play().then (() => {bplay.classList.add('hidden');})})});
